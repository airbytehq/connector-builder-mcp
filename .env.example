# FastMCP Cloud Deployment Configuration
# Copy this file to .env and configure the values for your deployment

# Authentication Configuration
# Required for secure remote access to your MCP server
# Generate a secure random token for production use
MCP_AUTH_TOKEN=your-secure-auth-token-here

# Server Configuration
# Port for the HTTP server (default: 8000)
# FastMCP Cloud will automatically configure this, but you can override if needed
PORT=8000

# Host binding configuration (default: 0.0.0.0 for external access)
# Use 0.0.0.0 to accept connections from any IP address
# Use 127.0.0.1 for localhost-only access (not recommended for cloud deployment)
HOST=0.0.0.0

# Optional: Additional configuration for advanced deployments
# Uncomment and configure as needed

# Logging level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
# LOG_LEVEL=INFO

# Custom health check configuration
# HEALTH_CHECK_ENABLED=true

# Example of how to generate a secure auth token:
# python -c "import secrets; print(secrets.token_urlsafe(32))"
